# pnpm configuration for better performance
# Enable faster installs and better caching
shamefully-hoist=false
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
public-hoist-pattern[]=*typescript*

# Reduce node_modules size by avoiding unnecessary hoisting
node-linker=isolated

# Keep pnpm store outside the repo to avoid watcher overhead
# Using the default pnpm store location
store-dir=/Users/stapo/Library/pnpm/store/v10
