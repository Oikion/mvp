/**
 * @deprecated
 *
 * This file is intentionally empty.
 *
 * The legacy `CreateDealButton` component was retired as part of the v2.0
 * Entity Architecture migration (Phase 3). Deal creation now flows through
 * `QuickAddDeal` (built by a separate task) and the deals wizard sheet
 * mounted inside `DealsPageView.tsx`.
 *
 * This stub remains only because the harness cannot delete files. Do not
 * import from it — it exports nothing.
 */
export {};
