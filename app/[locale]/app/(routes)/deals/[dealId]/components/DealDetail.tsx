/**
 * @deprecated
 *
 * Replaced by `DealView.tsx` as part of Phase 3 of the v2.0 Entity
 * Architecture migration. The 10-stage Greek RE pipeline, DealParty,
 * and DealStageLog features are not compatible with the legacy
 * `DealDetail` component, which assumed a 6-stage status enum and
 * direct clientId FK.
 *
 * Do not import from this file. It exists only as a stub because
 * the harness cannot delete files atomically.
 */
const DEPRECATED = true;
export default DEPRECATED;
